- use Postgre SQL in Codeigniter, table case sensitive must be lowecase typing
- Tips: use Backend or ServerSide to manipulate data, if use frontend to manipulate could be lagging or crash.
- Tips: use Ajax in some any trick you can do it
        - $.ajax({
            url = "your/url"
            method: "POST" or GET or ETC
            type: "JSON" or ETC
            data: {
                key: value
            }
        }).done(function(data) {
            console.log(data)
        }).fail(function(data) {
            console.log(data)
        }
        - $.post("your/url", { key:value }, function(data) {
            console.log(data)
        })
        - $.get("your/url", { key:value }, function(data) {
            console.log(data)
        })

- Tips: prop("disabled" , true) only works in select option
- Tips: Align is Horizontal if flex-col but is Vertical if flex-row
- Tips: if url not found the problem maybe is user agent different
- Tips: padding with background margin without background
- Tips: if input type checkbox cannot reserved submit change to hidden
- Tips: if error pgsql column doest not exists, delete column, and create new column
